name: Capistrano
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [master, dev]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ruby:2.6.6
    
    steps:
      - uses: actions/checkout@v2
      - name: setup bundle
        run: bundle install
